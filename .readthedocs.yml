version: 2

build:
   os: "ubuntu-22.04"
   tools:
      python: "3.8"
   jobs:
      pre_build:
      - cd brainscore_language/utils
      - python document_plugins.py


python:
   install:
      - requirements: docs/requirements.txt
